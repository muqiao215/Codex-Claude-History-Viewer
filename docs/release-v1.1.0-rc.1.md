# v1.1.0-rc.1

Candidate release; v1.0.0 remains stable. Independent first-time-user validation is pending.

- Codex cumulative and Claude message-deduplicated token accounting; no billing/quota promise.
- Daily deterministic briefings with explicit truncation, optional user-triggered AI interpretation.
- Read-only plan previews, keyboard-accessible session/project rows, preserved evidence navigation.
- Native OpenCode session resume command; CM v0.42.2 can separately supervise explicit adoption.
- Portable Node DOM fixtures and date-independent plan-window regression.

Run `python3 -m unittest discover -s tests -p 'test_*.py'` and each `tests/*.js` with Node.
Run `python3 app.py --version` to identify local source. Index parser version 5 rebuilds derived
Codex/Claude indexes without rewriting original histories. Back up Viewer-owned indexes before
rollback to v1.0.0; retain provider-owned history untouched. Windows/WSL and live LLM calls are
not newly certified by this release. Synthetic trial materials are under the product-validation plan.
