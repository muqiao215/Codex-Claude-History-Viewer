# 组织者记录（不要提前发给试用者）

## 数据及环境

早期专用试用 ZIP 是历史快照。当前候选使用 v1.1.0-rc.1 Release 的源码包，包含五个合成 JSONL；manifest.json 校验合成数据。记录 Release commit 和源码包哈希。向试用者提供任务单，但不要提前展示本判定参考。

先记录：试用编号、候选 commit、未提交 diff 标识/源码包哈希、Python/浏览器版本。使用干净 Linux 账户，确认无 ~/.codex、~/.claude、~/.openclaw、~/.hermes 和 ~/.local/share/opencode 历史，且源码旁无 hermes-agent/.hermes-home/state.db。不要删除日常账户的数据来满足此要求。现有自动发现逻辑会在显式 DB 路径不存在时回退，因此“指向不存在的 DB”不等于禁用来源。

## 判定参考

- 排序任务：demo-beta-0001；可搜索 `sort by last activity`，用户无需使用完全相同的检索方法。
- 失败任务：demo-alpha-0001；工具输出有 Exit code: 1、RuntimeError: demo error，还含一次中断。可以引用转录或审计中的可追溯证据，不能要求照抄固定措辞。
- 交接必须属于 alpha；核查 goal/错误或中断提醒与源会话一致，且两个文件能打开。无需评价主题审美。
- Claude 可选项：demo-claude-0001；工具失败为 rg: command not found。
- 不将无 token / 无计划空态判为错误；不要使用当天空简报判断历史数据丢失。

## 观察与通过标准

先给任务单及源码 README，不给答案或点击演示。允许观察和记录；求助时先问卡在哪里，所有提示逐条记录。完成耗时仅作基线，不设事后阈值。

M3 完成要求：至少一位未参与开发的人无需开发者逐步指导完成启动→检索→证据→交接导出，没有未处理的阻断问题。有提示的步骤标为“协助完成”；修复后再复走。无人试用时保持 pending，组织者/Agent 的预检不替代独立试用。

问题记录：编号 | 步骤 | 用户原话 | 预期/实际 | 严重度 | 复现 | 修复版本 | 复测结果。
本轮日期、来源和平台之外的能力仍未验证。不主动邀请、发消息、发布或收集个人历史。
