Viewer already supports source selection and platform-aware resume command display, but buildResumeInvocation omitted OpenCode. The installed native CLI supports opencode --session ID and opencode run --session ID. Neither requires a TaskHub task. Session continuity retains native dialogue state; SpecMesh retains verified project facts. Current ZAI invocation logs show five-hour quota exhaustion with repeated retries. Test the already configured MiniMax provider without changing the global default.

Native MiniMax resume persisted the expected historical answer. Browser verification also exposes previously created fork sessions, superseding the earlier inference that no stdout/no parent_id meant no fork. Native dialogue state must be checked independently from CLI event delivery and exit.
